var miVariable = 9;
var condicional = miVariable == 10;
var condicional2 = miVariable > 5;

var edad=0;
nombre='jose';
if(nombre){
    alert('Se cumple la condicion')
}else{
    alert('No se a cumplido la condicion')
}


// para concatenar se puede utilizar && que se deben cumplir todas la condicones.
//  tambien exite or " || " que se necesita una de la condiciones para que se cumpla la condicion.
// < > <= >= == !=
// negacion " ! ", la negacion lo que hace es converti un falso en un verdadero y un verdadero en falso.

// NOTA: si se pone un valor Positivo o Negativo, javascript lo interpreta como un valor verdadero pero si
// el numero es cero entonces lo interpreta como  falso